import { ScrollVibrationDirective } from './scroll-vibration.directive';

describe('ScrollVibrationDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollVibrationDirective();
    expect(directive).toBeTruthy();
  });
});
